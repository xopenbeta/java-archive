# Java Archive

自动构建和打包多版本、多平台的 Java Development Kit (JDK)。

## 📦 支持的平台

### Java 版本与发行版

| Java 版本 | 发行版 | 说明 |
|-----------|--------|------|
| Java 8-23 | Eclipse Temurin | 社区驱动，TCK 认证，统一使用 |
| Java 8 (macOS ARM64) | Azul Zulu | Temurin 不支持 macOS ARM64，使用 Zulu |

> **版本策略**: 
> - 使用主版本号（如 '8', '11', '17', '21'），自动获取最新的安全补丁版本
> - 确保始终使用最新的稳定和安全更新
> - Eclipse Temurin 为所有支持的版本提供一致的体验
> - **特例**: macOS ARM64 的 Java 8 使用 Azul Zulu，因为 Eclipse Temurin 没有提供此平台的 Java 8

### macOS (全部在 Apple Silicon 上编译)
- **x64** (适用于 Intel 芯片 Mac) - 在 macOS 14 上交叉编译
- **aarch64** (Apple Silicon M1/M2/M3/M4 原生支持) - 在 macOS 14 上编译

> **注意**: GitHub Actions 不再提供 Intel Mac runners，所有 macOS 构建现在都在 Apple Silicon 上进行。x64 版本通过交叉编译生成，完全支持 Intel Mac。

### Linux
- **x64** (AMD64)

### Windows
- **x64** (64位) - 在 Windows Server 2022 (x64) 上编译
- ~~**aarch64** (ARM 64位)~~ - **暂不可用**

> **Windows ARM64 说明**: GitHub Actions 目前没有提供公共的 Windows ARM64 runner。如需此平台支持，请使用 self-hosted runner。等待 GitHub 官方提供 `windows-11-arm64` 公共 runner 后将启用。

## 🤖 自动构建

### 触发条件

构建会在以下情况下自动触发：
**使用标签触发构建**:
```bash
git tag v202602121445
git push origin v202602121445
```

### 构建矩阵

工作流使用矩阵策略同时构建多个组合：

```yaml
java-versions: ['8', '11', '17', '21', '23']
platforms:
  - macOS x64 (在 Apple Silicon 上交叉编译)
  - macOS aarch64 (原生编译)
  - Linux x64
  - Windows x64
  # - Windows aarch64 (暂不可用 - 等待 GitHub Actions 公共 runner)
```

**总计: 28 个构建任务** - 每个 Java 版本支持 4 个主要平台。Windows ARM64 待 GitHub Actions 提供公共 runner 后启用。
```
